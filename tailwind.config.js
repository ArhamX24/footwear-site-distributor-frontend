/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Scan for Tailwind classes in the `src` folder
    ],
    theme: {
      
    },
    plugins: [
        
    ],
  };